import { TitleFromIdPipe } from './title-from-id-pipe';

describe('TitleFromIdPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleFromIdPipe();
    expect(pipe).toBeTruthy();
  });
});
